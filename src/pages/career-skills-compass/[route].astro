---
import Compass from '../career-skills-compass.astro';

export function getStaticPaths() {
  return ['direction', 'careers', 'transition', 'skills', 'method'].map((route) => ({ params: { route } }));
}

const { route } = Astro.params;
---

<Compass route={route} />
